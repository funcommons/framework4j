<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>fun.commons</groupId>
    <artifactId>framework4j</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Commons SDK</name>
    <description>企业内部通用工具包 - Spring Boot Starter</description>

    <modules>
        <module>framework4j-api</module>
        <module>framework4j-web</module>
        <module>framework4j-datetime</module>
        <module>framework4j-id</module>
        <module>framework4j-redis</module>
        <module>framework4j-datasource</module>
        <module>framework4j-sql-tracing</module>
        <module>framework4j-accesstoken</module>
        <module>framework4j-idempotency</module>
        <module>framework4j-signature</module>
        <module>framework4j-rate-limit</module>
        <module>framework4j-cache</module>
        <module>framework4j-audit</module>
        <module>framework4j-sensitive</module>
        <module>framework4j-compat-test</module>
        <module>framework4j-all</module>
    </modules>

    <!-- demo 模块仅在非 JitPack 环境下构建（本地/CI 需要运行示例，发布时跳过） -->
    <profiles>
        <profile>
            <id>demo</id>
            <activation>
                <property>
                    <name>!jitpack</name>
                </property>
            </activation>
            <modules>
                <module>framework4j-demo</module>
            </modules>
        </profile>
    </profiles>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Spring Boot -->
        <spring-boot.version>3.2.0</spring-boot.version>

        <!-- Redis -->
        <redisson.version>3.25.0</redisson.version>

        <!-- Database -->
        <druid.version>1.2.20</druid.version>
        <mybatis-plus.version>3.5.14</mybatis-plus.version>
        <postgresql.version>42.7.1</postgresql.version>

        <!-- Utils -->
        <lombok.version>1.18.30</lombok.version>
        <commons-lang3.version>3.14.0</commons-lang3.version>
        <commons-collections4.version>4.4</commons-collections4.version>
        <commons-pool2.version>2.12.0</commons-pool2.version>

        <!-- Test -->
        <junit.version>5.10.1</junit.version>
        <mockito.version>5.7.0</mockito.version>
        <byte-buddy.version>1.14.10</byte-buddy.version>
        <h2.version>2.2.224</h2.version>
        <assertj.version>3.25.3</assertj.version>
        <jacoco.version>0.8.11</jacoco.version>
        <jacoco.coverage.min>0.80</jacoco.coverage.min>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Redisson -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <!-- Druid -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- MyBatis Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- PostgreSQL -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>

            <!-- Lombok -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>

            <!-- Commons -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-collections4</artifactId>
                <version>${commons-collections4.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-pool2</artifactId>
                <version>${commons-pool2.version}</version>
            </dependency>

            <!-- H2 Database (Test Only) -->
            <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>${h2.version}</version>
            </dependency>

            <!-- ByteBuddy for Mockito -->
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy-agent</artifactId>
                <version>${byte-buddy.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- v2.1: AssertJ 统一断言（推荐首选） -->
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Commons Modules -->
              <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-api</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-web</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-signature</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-rate-limit</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-cache</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-audit</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-sensitive</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-redis</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-datasource</artifactId>
                <version>${project.version}</version>
            </dependency>
              <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-accesstoken</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>fun.commons</groupId>
                <artifactId>framework4j-all</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <!-- v2.1: -parameters 启用参数名（SpEL #id 解析 + Java 反射.getParameterName()） -->
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.springframework.boot</groupId>
                                <artifactId>spring-boot-configuration-processor</artifactId>
                                <version>${spring-boot.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>3.4.0</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>3.6.3</version>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <charset>${project.build.sourceEncoding}</charset>
                        <docencoding>${project.build.sourceEncoding}</docencoding>
                        <doclint>none</doclint>
                        <!-- 解决 Java 17 的 Javadoc 生成问题 -->
                        <source>17</source>
                        <!-- 跳过一些可能导致错误的检查 -->
                        <additionalparam>-Xdoclint:none</additionalparam>
                        <additionalJOption>-Xdoclint:none</additionalJOption>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>3.1.1</version>
                    <configuration>
                        <deployAtEnd>false</deployAtEnd>
                        <updateReleaseInfo>true</updateReleaseInfo>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.2</version>
                    <configuration>
                        <argLine>
                            -XX:+EnableDynamicAgentLoading
                            -Djdk.attach.allowAttachSelf=true
                        </argLine>
                        <!-- v2.1: 默认排除 @Tag("performance") 性能测试，加速日常 CI -->
                        <!-- 跑性能测试：mvn test -Dgroups=performance -->
                        <excludedGroups>performance</excludedGroups>
                    </configuration>
                </plugin>

                <!-- v2.1: JaCoCo 覆盖率（mvn verify 时检查 ≥ 60%，可调） -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals><goal>prepare-agent</goal></goals>
                        </execution>
                        <execution>
                            <id>check</id>
                            <goals><goal>check</goal></goals>
                            <phase>verify</phase>
                            <configuration>
                                <rules>
                                    <rule>
                                        <element>BUNDLE</element>
                                        <limits>
                                            <limit>
                                                <counter>LINE</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>${jacoco.coverage.min}</minimum>
                                            </limit>
                                        </limits>
                                    </rule>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- v2.2: Maven Enforcer 依赖冲突检测（防 jsqlparser 类版本冲突） -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>3.4.1</version>
                    <executions>
                        <execution>
                            <id>enforce-dependency-rules</id>
                            <goals><goal>enforce</goal></goals>
                            <configuration>
                                <rules>
                                    <dependencyConvergence/>
                                    <requireUpperBoundDeps/>
                                    <bannedDependencies>
                                        <excludes>
                                            <exclude>com.alibaba:fastjson2</exclude>
                                            <exclude>com.alibaba:fastjson</exclude>
                                        </excludes>
                                        <message>禁用 fastjson/fastjson2（autotype RCE 风险，统一用 Jackson）</message>
                                    </bannedDependencies>
                                </rules>
                                <fail>true</fail>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
