# Framework4j 架构规划文档 v2.0

> 本文记录 v2.0.0 重构完成后的最终架构：模块拆分、命名约定、目录结构、依赖关系。
>
> **制定日期**：2026-07-04
> **当前版本**：2.0.0-SNAPSHOT
> **目标版本**：2.0.0 GA

---

## 一、设计原则

1. **单一职责**：每个 Maven 模块只承担一个明确职责（api / id / datetime / redis / datasource / sql-tracing / accesstoken / idempotency / all）。
2. **可插拔**：所有模块通过 `@ConditionalOnProperty(prefix="framework4j.<module>", name="enabled")` 开关，默认按需启用。
3. **顶级 BeanPostProcessor**：`@RedisOn` / `@DataSourceOn` / `@RequiresToken` 处理器必须是顶级类（非内嵌静态类），避免 Spring BeanPostProcessor 加载顺序警告。
4. **Jackson 统一**：禁 fastjson2、禁 commons-lang3 StringUtils（用 Spring 的）、禁 commons-codec HmacUtils（用 JDK `javax.crypto.Mac` + ThreadLocal 缓存）。
5. **配置前缀**：所有 `@ConfigurationProperties` 走 `framework4j.<module>.*` 前缀；accesstoken 用 kebab-case `framework4j.access-token.*`。
6. **包名约定**：`fun.commons.framework4j.<module>`，与目录名对齐。仅 accesstoken 用 `fun.commons.framework4j.accesstoken`（历史）。

---

## 二、模块清单（10 个）

| # | 模块名 | artifactId | 职责 | 配置前缀 | 测试数 |
|---|---|---|---|---|---|
| 1 | api | `framework4j-api` | ApiResponse/ApiCode/ApiAssert/GlobalExceptionHandler/TraceContext + Jackson WebConfig | `framework4j.api.config.enabled` | 66（12 skipped） |
| 2 | datetime | `framework4j-datetime` | OffsetDateTime 序列化 / TimeFormatInterceptor / 时间转换器 | `framework4j.datetime.enabled` | 39 |
| 3 | id | `framework4j-id` | Snowflake + OpenID 混淆 + WorkerIdStrategy (Redis/IP-hash) | `framework4j.id.*`、`framework4j.openid.*` | 99 |
| 4 | sql-tracing | `framework4j-sql-tracing` | TraceIdDruidFilter + DefaultTraceIdProvider + 3 种 TracingMode | `framework4j.datasource.sql-tracing.*` | 33 |
| 5 | datasource | `framework4j-datasource` | 多 DataSource + `@DataSourceOn` + Druid + MyBatis Plus | `framework4j.datasource.*` | 123 |
| 6 | redis | `framework4j-redis` | 多 Redis + `@RedisOn` + Lettuce/Redisson 双客户端 + Jackson 序列化器 | `framework4j.redis.*` | 99 |
| 7 | accesstoken | `framework4j-accesstoken` | JWT + Redis 双验 + access/refresh family + 拆分 access/refresh strategy | `framework4j.access-token.*` | 88 |
| 8 | idempotency | `framework4j-idempotency` | `Idempotency-Key` + Redis 48h 保留 | `framework4j.idempotency.*` | 5 |
| 9 | all | `framework4j-all` | 聚合 starter，引入 api + datetime + id + sql-tracing + datasource + redis + accesstoken + idempotency | — | — |
| 10 | (parent) | `framework4j` | Maven 父 POM，统一依赖管理（BOM） | — | — |

**测试汇总**：522 通过，0 失败，0 错误，12 skipped（fastjson2 残留）

---

## 三、目录结构

```
framework4j/
├── pom.xml                          # 父 POM (BOM 管理)
├── CLAUDE.md                        # AI 助手指南
├── 第二期规划.md                    # 路线图
├── 架构规划文档 v2.0.md             # 本文
├── .claude/skills/                  # mc-* 规范文档（14 份 SKILL）
│
├── framework4j-api/                 # ← 原 framework4j-core
│   ├── pom.xml                      # artifactId: framework4j-api
│   └── src/main/java/fun/commons/framework4j/api/
│       ├── ApiResponse.java
│       ├── ApiCode.java
│       ├── ApiAssert.java
│       ├── ApiException.java
│       ├── GlobalExceptionHandler.java
│       ├── TraceContext.java
│       ├── TraceConfig.java
│       └── config/
│           └── WebConfig.java       # ← 原 core.config.WebConfig
│
├── framework4j-datetime/
│   └── src/main/java/fun/commons/framework4j/datetime/
│       ├── config/DateTimeAutoConfiguration.java
│       ├── serializer/OffsetDateTimeSerializer.java
│       └── interceptor/TimeFormatInterceptor.java
│
├── framework4j-id/
│   └── src/main/java/fun/commons/framework4j/
│       ├── id/...                   # Snowflake + WorkerIdStrategy
│       └── openid/...               # OpenID 混淆
│
├── framework4j-sql-tracing/         # ← 从 datasource 拆出
│   └── src/main/java/fun/commons/framework4j/datasource/tracing/
│       ├── SqlTracingAutoConfiguration.java
│       ├── SqlTracingProperties.java
│       ├── TraceIdDruidFilter.java
│       ├── TraceIdProvider.java
│       └── DefaultTraceIdProvider.java
│
├── framework4j-datasource/
│   └── src/main/java/fun/commons/framework4j/datasource/
│       ├── annotation/DataSourceOn.java
│       ├── config/
│       │   ├── MultiDataSourceAutoConfiguration.java
│       │   └── DataSourceOnBeanPostProcessor.java   # ← 顶级类
│       ├── manager/MultiDataSourceManager.java
│       └── properties/...
│
├── framework4j-redis/
│   └── src/main/java/fun/commons/framework4j/redis/
│       ├── annotation/RedisOn.java
│       ├── config/
│       │   ├── MultiRedisAutoConfiguration.java
│       │   └── RedisOnBeanPostProcessor.java        # ← 顶级类
│       ├── manager/MultiRedisManager.java
│       └── serializer/JsonRedisSerializer.java       # ← Jackson 替代 fastjson2
│
├── framework4j-accesstoken/
│   └── src/main/java/fun/commons/framework4j/accesstoken/
│       ├── annotation/RequiresToken.java             # type() 字段：access/refresh
│       ├── config/
│       │   ├── AccessTokenAutoConfiguration.java
│       │   ├── AccessTokenProperties.java            # pathPatterns / excludePathPatterns
│       │   └── AccessTokenWebMvcConfig.java          # ← 路径可配置
│       ├── context/TokenContext.java
│       ├── core/
│       │   ├── AccessTokenGenerator.java             # ← 仅 access 路径
│       │   ├── RefreshTokenService.java              # ← 新：family + Lua 轮转
│       │   └── TokenKeyBuilder.java                  # ← 5 类 Redis Key 集中
│       ├── interceptor/
│       │   ├── TokenInterceptor.java                 # ← 仅路由
│       │   ├── AccessTokenValidationStrategy.java    # ← 拆出
│       │   └── RefreshTokenValidationStrategy.java   # ← 拆出
│       ├── exception/AuthException.java
│       └── util/TokenUtils.java                      # ← ThreadLocal<Mac> 缓存
│
├── framework4j-idempotency/
│   └── src/main/java/fun/commons/framework4j/idempotency/...
│
└── framework4j-all/                  # 聚合
    └── pom.xml
```

---

## 四、模块依赖图

```
                          ┌──────────────────┐
                          │  framework4j-all │
                          │  (aggregator)    │
                          └────────┬─────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        ▼                          ▼                          ▼
┌───────────────┐         ┌───────────────┐          ┌───────────────┐
│  accesstoken  │◄───────►│     redis     │◄────────►│  datasource   │
│  (refresh)    │         │ (@RedisOn)    │          │ (@DataSourceOn)│
└───────┬───────┘         └───────┬───────┘          └───────┬───────┘
        │                         │                          │
        │                         │                          │
        ▼                         ▼                          ▼
┌───────────────┐         ┌───────────────┐          ┌───────────────┐
│      api      │◄────────│  sql-tracing  │◄─────────│  datetime     │
│ (ApiResponse) │         │ (Druid Filter)│          │ (OffsetDateTime)│
└───────────────┘         └───────────────┘          └───────────────┘
        ▲
        │
        │
┌───────┴───────┐         ┌───────────────┐
│      id       │         │ idempotency   │
│ (Snowflake)   │         │ (Idempotency) │
└───────────────┘         └───────────────┘
```

**关键依赖**：
- `accesstoken` → `redis`（必须，StringRedisTemplate 走 MultiRedisManager）
- `datasource` → `sql-tracing`（TraceIdDruidFilter 被 BeanPostProcessor 注入）
- `id` → `api`（仅 test scope，运行时独立）
- `all` → 全部 7 个功能模块

---

## 五、命名约定

| 类别 | 约定 | 示例 |
|---|---|---|
| 模块目录 | `framework4j-<module>` | `framework4j-api`、`framework4j-accesstoken` |
| artifactId | 同目录名 | `framework4j-api` |
| 包名 | `fun.commons.framework4j.<module>` | `fun.commons.framework4j.redis` |
| 配置前缀 | `framework4j.<module>.*` | `framework4j.access-token.*`（kebab-case） |
| AutoConfiguration | `<Module>AutoConfiguration` | `MultiRedisAutoConfiguration` |
| Properties | `<Module>Properties` | `AccessTokenProperties` |
| BeanPostProcessor | 顶级类 `<Annotation>BeanPostProcessor` | `RedisOnBeanPostProcessor` |
| 注解 | `@<Action>On` | `@RedisOn`、`@DataSourceOn`、`@RequiresToken` |
| 工具类 | `<Domain>Utils` | `TokenUtils`、`TokenKeyBuilder` |
| 测试目录 | `src/test/java/.../<module>/` 分 `unit/`、`functional/`、`integration/`、`performance/` | — |

---

## 六、关键演进（v1.x → v2.0）

| 演进项 | v1.x | v2.0 | 收益 |
|---|---|---|---|
| core 模块职责 | 3741 行，混合 api/id/openid/datetime | 998 行 → 拆为 api + datetime + id 独立模块 | 单一职责，按需引入 |
| JSON 库 | fastjson2 + Jackson 混用 | Jackson 统一 | 消除 autotype RCE 风险 |
| BeanPostProcessor | 内嵌静态类 | 顶级类 | 消除 Spring 加载警告 |
| accesstoken refresh | AccessTokenGenerator 单类含 refresh | 拆 AccessTokenGenerator + RefreshTokenService + TokenKeyBuilder | 单类 < 200 行 |
| TokenInterceptor | 1 类 254 行 | TokenInterceptor 路由 + AccessToken/RefreshValidationStrategy | 策略模式可扩展 |
| sql-tracing | 内嵌于 datasource | 独立模块 + 自己的 AutoConfiguration | 可单独引入 |
| Redis Key 构造 | 散落在 4 个类中字符串拼接 | TokenKeyBuilder 集中 5 类 | 一致性 |
| HMAC 签名 | commons-codec HmacUtils | JDK `javax.crypto.Mac` + ThreadLocal 缓存 | 减依赖 + 性能 |
| 拦截器路径 | 硬编码 `/**` | `pathPatterns` / `excludePathPatterns` 可配置 | 灵活 |

---

## 七、不在 v2.0 范围

- **JJWT 迁移**：当前 TokenUtils 自实现 HS256，未引入 JJWT。评估：JJWT 提供 RS256/ES256 但增依赖。v2.0 保留自实现。
- **Spring Security 集成**：TokenContext → SecurityContextHolder 桥接由消费者应用负责。
- **OAuth2 第三方登录**：微信/钉钉/GitHub 由消费者应用自实现。
- **审计日志表**：append-only + hash chain 由消费者应用负责。
- **PostgreSQL 专用功能**：framework4j-datasource 仅做 Druid + MyBatis Plus 抽象，不绑特定数据库。

---

## 八、质量基线

- 单元测试：522 通过，0 失败，0 错误，12 skipped
- 模块构建：10/10 SUCCESS
- 规范文档：14 份 SKILL（mc-java-spec / mc-api-spec / mc-java-security / mc-doc-* / mc-perf 等）
- 包名/前缀/groupId：零 `com.framework4j` 残留
- 代码/文档/测试：三方一致性已核查
