# Framework4j 第二期规划

> 本文记录 v2.0.0 重构完成后的最新规划。原 v1.x 路线图已部分完成，剩余项重新评估优先级。
>
> **制定日期**：2026-07-03
> **最近更新**：2026-07-04（v2.0.0 重构完成、core 改名 api、sql-tracing 拆出独立模块、accesstoken 拆 access/refresh strategy）
> **当前版本**：2.0.0-SNAPSHOT
> **目标版本**：2.0.0 GA

---

## 一、现状基线

### 1.1 v2.0.0 已完成能力（10 模块）

| 模块 | 能力 | 成熟度 | 测试数 |
|---|---|---|---|
| `framework4j-api`（原 core） | ApiResponse/ApiCode/GlobalExceptionHandler + TraceContext + Jackson WebConfig | ★★★★ | 66（12 skipped） |
| `framework4j-datetime` | OffsetDateTime 序列化 / TimeFormatInterceptor | ★★★★ | 39 |
| `framework4j-id` | Snowflake + OpenID 混淆 + WorkerIdStrategy | ★★★★ | 99 |
| `framework4j-sql-tracing` | TraceIdDruidFilter + 3 种 TracingMode + DefaultTraceIdProvider | ★★★★ | 33 |
| `framework4j-datasource` | 多 DataSource + `@DataSourceOn` + Druid + MyBatis Plus | ★★★★ | 123 |
| `framework4j-redis` | 多 Redis + `@RedisOn` + Lettuce/Redisson + Jackson 序列化器 | ★★★★ | 99 |
| `framework4j-accesstoken` | JWT + Redis 双验 + access/refresh family + 拆分 strategy | ★★★★★ | 88 |
| `framework4j-idempotency` | `Idempotency-Key` + Redis 48h 保留 | ★★★ | 5 |
| `framework4j-all` | 聚合 starter | ★★★ | — |
| **合计** | **10 模块** | — | **522 通过 / 12 skipped** |

### 1.2 v2.0.0 重构成果

| 演进项 | v1.x | v2.0 | 状态 |
|---|---|---|---|
| core 模块职责 | 3741 行混合 | 拆为 api + datetime + id 三模块 | ✅ |
| JSON 库 | fastjson2 + Jackson 混用 | Jackson 统一（消除 autotype RCE） | ✅ |
| BeanPostProcessor | 内嵌静态类 | 顶级类（消除 Spring 警告） | ✅ |
| accesstoken refresh | 单类混合 | AccessTokenGenerator + RefreshTokenService + TokenKeyBuilder 拆分 | ✅ |
| TokenInterceptor | 254 行单类 | TokenInterceptor 路由 + AccessToken/RefreshValidationStrategy | ✅ |
| sql-tracing | 内嵌 datasource | 独立模块 + AutoConfiguration | ✅ |
| Redis Key 构造 | 散落字符串拼接 | TokenKeyBuilder 集中 5 类 | ✅ |
| HMAC 签名 | commons-codec | JDK `javax.crypto.Mac` + ThreadLocal 缓存 | ✅ |
| 拦截器路径 | 硬编码 `/**` | pathPatterns / excludePathPatterns 可配置 | ✅ |
| sql-tracing 测试 | 0 | 33 单元测试（properties / filter / autoconfig / provider） | ✅ |
| core 改名 | framework4j-core | framework4j-api（artifactId + 目录 + 包名 core.config → api.config） | ✅ |

### 1.3 质量基线

- 单元测试：522 通过，0 失败，0 错误，12 skipped（fastjson2 残留测试待重写）
- 模块构建：10/10 SUCCESS
- 规范文档：14 份 SKILL（mc-java-spec / mc-api-spec / mc-java-security / mc-doc-* / mc-perf）
- 包名/前缀/groupId：零 `com.framework4j` 残留
- 代码/文档/测试：三方一致性已核查
- 详细架构：见 [架构规划文档 v2.0.md](./架构规划文档%20v2.0.md)

---

## 二、定位与策略

### 2.1 产品定位

**企业级 Spring Boot 基础设施 SDK —— 安全 + 规范 + 可插拔**

不与 ruoyi/pig/ballcat 竞争（它们是应用系统），定位为：
- Spring Boot 官方 starter 的企业级补全
- 可被应用系统（ruoyi/mall/自研后台）集成的基础设施层
- 配套 AI 可调用的规范文档体系

### 2.2 差异化资产

| 资产 | 同类产品 | framework4j |
|---|---|---|
| 多 Redis 注解注入 | 无 | `@RedisOn` 类级/字段级 |
| 多 DataSource 注解注入 | 简单 | `@DataSourceOn` 类级/字段级 |
| JWT family + 重用检测 | 无 | Lua + 毒丸 + maxRotations + 拆分 RefreshTokenService |
| OpenID 混淆 | 无 | 12 字符 + 校验位 + TypeHandler |
| SQL 追踪 | 无 | Druid Filter 注入 trace_id（独立模块可单独引入） |
| 配套规范文档 | 无 | 14 份 SKILL（可被 Claude Code 调用） |
| 拦截器策略模式 | 无 | AccessToken/RefreshValidationStrategy 可扩展 |

### 2.3 不做的事

- ❌ RBAC / 权限模型（ruoyi/pig 领域）
- ❌ 工作流 / 代码生成（应用层）
- ❌ 完整后台管理系统（非基础设施）
- ❌ 微服务治理（Spring Cloud 生态）

---

## 三、第二期建设目标（重新评估）

### 3.1 v2.0.0 GA 收尾（立即）

| # | 项 | 优先级 | 说明 |
|---|---|---|---|
| 1 | 12 个 skipped 测试重写 | P0 | 原 fastjson2 相关测试，改 Jackson 后 @Disabled，需重写覆盖 Jackson snake_case + Long→String |
| 2 | accesstoken 模块用户指南更新 | P0 | 反映 RefreshTokenService 拆分 + TokenKeyBuilder + pathPatterns 配置 |
| 3 | api 模块文档迁移 | P0 | 原 framework4j-core/ 下的 7 份 .md 文档迁移到 framework4j-api/，并更新引用 |
| 4 | JJWT 迁移评估 | P1 | 当前自实现 HS256；评估引入 JJWT 支持 RS256/ES256（增依赖 vs 功能） |

### 3.2 补齐基础设施（按优先级）

#### 🔴 P0 — 契约完整性（错误码已定义但无实现）

| # | 模块 | 能力 | 对应错误码 | 目标版本 |
|---|---|---|---|---|
| 1 | `framework4j-ratelimit` | 限流（Redis + Lua 滑动窗口 + 令牌桶） | 10500 `TOO_MANY_REQUESTS` + `Retry-After` 头 | 2.1.0 |
| 2 | `framework4j-openapi` | springdoc-openapi 统一封装 + OpenID/ApiResponse 适配 | — | 2.1.0 |

#### 🟠 P1 — 安全基础设施

| # | 模块 | 能力 | 对应错误码 | 目标版本 |
|---|---|---|---|---|
| 3 | `framework4j-signature` | HMAC-SHA256 接口签名 + timestamp + nonce 防重放 | 10302 `SIGNATURE_ERROR` / 10501 `DUPLICATE_SUBMIT` | 2.2.0 |
| 4 | `framework4j-upload` | 文件上传安全（Magic Number + UUID 重命名 + 后缀白名单 + OSS 独立域名） | 10503-10506 文件上传码 | 2.2.0 |
| 5 | `framework4j-cache` | 多级缓存（Caffeine + Redis）+ 防穿透/击穿/雪崩 + `@CacheOn` 注解 | — | 2.2.0 |

#### 🟡 P2 — 可观测与运维

| # | 模块 | 能力 | 目标版本 |
|---|---|---|---|
| 6 | `framework4j-audit` | 审计日志（append-only + hash chain + `@Auditable` AOP） | 2.3.0 |
| 7 | `framework4j-async` | 异步任务（ThreadPoolTaskExecutor 封装 + ShedLock + 异常处理） | 2.3.0 |
| 8 | `framework4j-metrics` | Micrometer 自定义指标 + Prometheus 暴露 + Grafana Dashboard 模板 | 2.3.0 |

#### 🟢 P3 — 工具与质量

| # | 模块 | 能力 | 目标版本 |
|---|---|---|---|
| 9 | `framework4j-password` | bcrypt cost 12 + 强度校验 + 登录锁定（Redis 计数） | 2.4.0 |
| 10 | `framework4j-testkit` | Testcontainers + ArchUnit 架构约束 + JaCoCo 门槛（≥ 80%） | 2.4.0 |

### 3.3 差异化强化

| 维度 | 强化方向 |
|---|---|
| **规范文档体系** | 14 份 SKILL 做成可被 Claude Code 调用的 AI 编程规范资产，支持 `/mc-java-spec` 等斜杠命令按规范生成代码 |
| **注解体系扩展** | 补 `@CacheOn` / `@QueueOn`（MQ）/ `@RateLimit` 同类注解，统一多资源注入范式 |
| **JWT family 旗舰特性** | 重点宣传——同类产品几乎无 family + 重用检测 + maxRotations + RefreshTokenService 拆分 |
| **OpenID 安全特性** | 重点宣传——12 字符 + 校验位 + TypeHandler + Swagger 全链路适配 |
| **SQL 追踪深度** | 重点宣传——Druid Filter 注入 trace_id，独立模块可单独引入 |
| **策略模式可扩展** | AccessToken/RefreshValidationStrategy 拆分，便于消费者自定义策略 |

### 3.4 文档与示例

| 项 | 现状 | 目标 |
|---|---|---|
| **示例项目** | 无 | 建 `framework4j-demo` 模块，演示完整应用组装 |
| **迁移指南** | 无 | 写"从 ruoyi/mall 迁移到 framework4j"指南 |
| **对比文档** | 无 | README 加"与 ruoyi/pig/ballcat 对比"表格 |
| **版本管理** | 2.0.0-SNAPSHOT | 发 2.0.0 GA + SemVer 规范 |
| **架构规划文档** | ✅ 已写 [架构规划文档 v2.0.md](./架构规划文档%20v2.0.md) | 持续维护 |

### 3.5 质量加固

| 项 | 现状 | 目标 |
|---|---|---|
| **测试覆盖** | 522 测试 | 加 JaCoCo 门槛 ≥ 80% + ArchUnit 架构约束 |
| **CI/CD** | 无 | GitHub Actions：push → build + test + 覆盖率上报 |
| **依赖更新** | 手动 | Dependabot / renovate |
| **安全扫描** | 无 | OWASP dependency-check |
| **sql-tracing 测试** | ✅ 33 个（properties/filter/autoconfig/provider） | 持续维护 |

---

## 四、路线图

### 4.1 里程碑

| 版本 | 主题 | 内容 | 预计 |
|---|---|---|---|
| **2.0.0 GA** | 重构收尾 | 10 模块 + 12 skipped 重写 + 文档迁移 | 立即 |
| **2.1.0** | 契约完整 | ratelimit + openapi | +1 个月 |
| **2.2.0** | 安全基础 | signature + upload + cache | +2 个月 |
| **2.3.0** | 可观测 | audit + async + metrics | +2 个月 |
| **2.4.0** | 工具质量 | password + testkit + JaCoCo 门槛 | +1 个月 |

### 4.2 模块关系图

#### 4.2.1 当前依赖关系（2.0.0）

```
                          ┌──────────────────┐
                          │  framework4j-all │
                          │  (aggregator)    │
                          └────────┬─────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        ▼                          ▼                          ▼
┌───────────────┐         ┌───────────────┐          ┌───────────────┐
│  accesstoken  │◄───────►│     redis     │◄────────►│  datasource   │
│ (refresh svc) │         │ (@RedisOn)    │          │ (@DataSourceOn)│
└───────┬───────┘         └───────┬───────┘          └───────┬───────┘
        │                         │                          │
        ▼                         ▼                          ▼
┌───────────────┐         ┌───────────────┐          ┌───────────────┐
│      api      │◄────────│  sql-tracing  │◄─────────│  datetime     │
│ (ApiResponse) │         │ (Druid Filter)│          │ (OffsetDateTime)│
└───────┬───────┘         └───────────────┘          └───────────────┘
        ▲
        │
┌───────┴───────┐         ┌───────────────┐
│      id       │         │ idempotency   │
│ (Snowflake)   │         │ (Idempotency) │
└───────────────┘         └───────────────┘
```

#### 4.2.2 v2.1.0+ 扩展（虚线为未来模块）

```
                  ┌──────────────────┐
                  │ framework4j-ratelimit (2.1.0) │ ← @RateLimit 注解
                  └──────────────────┘
                  ┌──────────────────┐
                  │ framework4j-signature (2.2.0) │ ← @Signature 注解
                  └──────────────────┘
                  ┌──────────────────┐
                  │ framework4j-cache (2.2.0) │ ← @CacheOn 注解
                  └──────────────────┘
                  ┌──────────────────┐
                  │ framework4j-audit (2.3.0) │ ← @Auditable AOP
                  └──────────────────┘
```

所有新模块遵循 v2.0 约定：
- 顶级 BeanPostProcessor / AutoConfiguration
- `framework4j.<module>.*` 配置前缀
- Jackson 序列化（禁 fastjson2）
- `@ConditionalOnProperty` 开关
- 5 类测试目录（unit / functional / integration / performance / manual）

---

## 五、变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-07-03 | v1.0 草稿 | 初版路线图，6 模块基线 |
| 2026-07-04 | v2.0 重构 | core → api 改名；datetime / id / sql-tracing 拆出独立模块；accesstoken 拆 RefreshTokenService + Strategy；fastjson2 → Jackson 全量迁移；522 测试通过 |
